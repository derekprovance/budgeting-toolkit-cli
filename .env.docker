# Docker Environment Configuration for Firefly III Development
# Copy this to .env.docker.local and customize as needed

# Firefly III Application Key (must be exactly 32 characters)
# Generate with: head /dev/urandom | LC_ALL=C tr -dc 'A-Za-z0-9' | head -c 32 && echo
FIREFLY_APP_KEY=SomeRandomStringOf32CharsExactly

# Firefly III API Token (generate after first login via Firefly UI)
# Go to: Options -> Profile -> OAuth -> Personal Access Tokens
FIREFLY_API_TOKEN=

# Database Configuration
POSTGRES_USER=firefly
POSTGRES_PASSWORD=firefly_secret
POSTGRES_DB=firefly

# Ports
FIREFLY_PORT=8080
IMPORTER_PORT=8081
ADMINER_PORT=8082
POSTGRES_PORT=5432

# Default Admin User (created on first run)
# Username: changeme@example.com
# Password: Set during first login
