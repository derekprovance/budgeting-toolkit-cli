FIREFLY_API_URL=https://your-firefly-instance.com
FIREFLY_API_TOKEN=your_api_token_here

LOG_LEVEL=silent

#################################
# Client Certificate (Optional)
#################################
# Required for mTLS authentication to Firefly III

# Path to CA certificate
# - In production: Use absolute paths (e.g., /etc/firefly-client/ca.pem)
# - In development: Relative paths resolve from project root
# - IMPORTANT: If using self-signed certificates, you can leave this blank
#   and TLS validation will be automatically disabled
# - If provided, TLS validation will be enabled for security
CLIENT_CERT_CA_PATH=

# Path to client certificate (supports PEM, CRT, P12, or PFX formats)
# - P12/PFX: Contains both certificate and private key (requires password)
# - PEM/CRT: Certificate file (requires separate .key file with same name)
CLIENT_CERT_PATH=../certs/client.p12

# Password for P12/PFX certificates (optional for PEM)
# WARNING: Stored in plaintext. Use secrets management in production.
CLIENT_CERT_PASSWORD=

# TLS certificate validation (optional override)
# - If not set: Automatically enabled when CA cert provided, disabled otherwise
# - Set to 'true' to force validation even without CA cert (will fail for self-signed)
# - Set to 'false' to force disable validation (NOT recommended for production)
# - Leave unset for intelligent default behavior
# STRICT_TLS=

#################################
# LLM
#################################
ANTHROPIC_API_KEY=your_anthropic_key

# LLM configuration (model, temperature, etc.) is in budgeting-toolkit.config.yaml, not .env